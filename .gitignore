# dependencias
/node_modules
/.pnp
.pnp.*

# build
/.next/
/out/
/build
*.tsbuildinfo
next-env.d.ts

# entorno — NUNCA versionar claves
# Patrones amplios: cualquier archivo que empiece o termine en "env" queda fuera,
# sin importar la carpeta. Un .gitignore estrecho ya casi publica las claves una vez.
.env
.env.*
*.env
.env*
**/.env*
**/*envsupabase*
**/credenciales*
**/secrets*

# supabase local
/supabase/.branches
/supabase/.temp
/supabase/.env

# sistema
.DS_Store
Thumbs.db
*.pem
npm-debug.log*
yarn-error.log*
.vercel
